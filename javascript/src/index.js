import { createDefault, isPlainObject } from './general.js';
import { runOnWorker, InlineWorker } from './run-on-worker.js';

export { createDefault, isPlainObject, runOnWorker, InlineWorker };

export default { createDefault, isPlainObject, runOnWorker, InlineWorker };
